diff --git a/dist/model/FileTreeController.js b/dist/model/FileTreeController.js
index 37c5d1ffedd5aff0258dec41b1b5cedb240aa3a6..2ce10e9b35769a8e2c2c5cf2a0b4f5a60bd5a8b8 100644
--- a/dist/model/FileTreeController.js
+++ b/dist/model/FileTreeController.js
@@ -1146,7 +1146,8 @@ var FileTreeController = class {
 			}
 			if (this.#dragSession != null && isPathMutationEvent(event)) this.#dragSession = null;
 			const focusPathCandidate = isPathMutationEvent(event) ? this.#applyMutationState(event) : this.#focusedPath;
-			const searchFocusCandidate = this.#searchValue != null && this.#searchValue.length > 0 ? this.#refreshActiveSearchState() : this.#searchValue === "" ? this.#focusedPath : focusPathCandidate;
+			const isExpansionEvent = event.operation === "expand" || event.operation === "collapse";
+			const searchFocusCandidate = this.#searchValue != null && this.#searchValue.length > 0 ? isExpansionEvent ? this.#focusedPath : this.#refreshActiveSearchState() : this.#searchValue === "" ? this.#focusedPath : focusPathCandidate;
 			const shouldBuildFullProjection = this.#searchValue != null || event.operation !== "expand" && event.operation !== "collapse";
 			this.#rebuildVisibleProjection(searchFocusCandidate, shouldBuildFullProjection);
 			this.#emit();
diff --git a/dist/render/FileTreeView.js b/dist/render/FileTreeView.js
index 648639b4b65c1dd797434e3afe189848426f4d53..907e52df55ce0fe8230c399e8a8d7ec14a708469 100644
--- a/dist/render/FileTreeView.js
+++ b/dist/render/FileTreeView.js
@@ -1671,7 +1671,7 @@ function FileTreeView({ composition, controller, gitStatusByPath, ignoredGitDire
 				shiftKey: event.shiftKey
 			},
 			isDirectory: row.kind === "directory",
-			isSearchOpen,
+			isSearchOpen: searchEnabled && isSearchOpen,
 			mode
 		});
 		const shouldToggleDirectory = plan.toggleDirectory && row.kind === "directory";
@@ -1706,7 +1706,8 @@ function FileTreeView({ composition, controller, gitStatusByPath, ignoredGitDire
 		isSearchOpen,
 		layoutSnapshot.visible.endIndex,
 		layoutSnapshot.visible.startIndex,
-		revealCanonicalRowAtStickyOffset
+		revealCanonicalRowAtStickyOffset,
+		searchEnabled
 	]);
 	const openMenuFromTrigger = () => {
 		if (isScrollingRef.current) return;
